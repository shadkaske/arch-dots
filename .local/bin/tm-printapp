#!/bin/bash

session="printApp"

sessionexists=$($tmux ls | grep $session)

if [ "$sessionexists" = "" ]
then
    # Start the tmux session detached
    tmux new-session -d -s $session

    # Setup editor window
    tmux rename-window -t 1 'Editor'
    tmux send-keys -t 'Editor' 'cd ~/Code/printapp' C-m 'vagrant ssh' C-m 'cd printapp' C-m 'nvim' C-m

    # setup server window
    tmux new-window -t $session:2 -n 'Server'
    tmux send-keys -t 'Server' '~/Code/printapp' C-m 'vagrant up' C-m 'vagrant ssh' C-m 'cd printapp' C-m 'clear' C-m

    # start up a lazygit session
    tmux new-window -t $session:3 -n 'LazyGit'
    tmux send-keys -t 'LazyGit' '~/Code/printapp' C-m 'lazygit' C-m

fi

st -t printApp -e tmux attach-session -t $session:1 -c $HOME/Code/printapp

# vi: ft=sh
